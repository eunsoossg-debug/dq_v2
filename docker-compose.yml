services:
  dq:
    image: dq-gui
    environment:
      DISPLAY: ${DISPLAY}
      QT_X11_NO_MITSHM: "1"
      XAUTHORITY: ${XAUTHORITY}
    volumes:
      - /tmp/.X11-unix:/tmpㅊ/.X11-unix:rw
      - ${XAUTHORITY}:${XAUTHORITY}:ro
      - ./:/app
    network_mode: host