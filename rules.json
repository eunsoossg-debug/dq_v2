{
  "evaluation_rules": {
    "3_syntax_validity": {
      "description": "Chemical은 단일 알파벳 형식, Background는 텍스트 형식 확인",
      "columns": {
        "Measurement_No": "^\\d+$",
        "Chemical": "^[A-Z]$",
        "Background": "^[a-zA-Z]+$"
      }
    },
    "4_semantic_validity": {
      "description": "Chemical은 F, C, O, N 중 하나, Background는 Blackbody 고정",
      "columns": {
        "Chemical": ["F", "C", "O", "N"],
        "Background": ["Blackbody"]
      }
    },
    "5_range_validity": {
      "description": "온도 및 센서값(640~400) 물리적 한계 확인",
      "columns": {
        "Temperature_C": { "min": -50, "max": 50 }
      },
      "column_groups": [
        {
          "match_regex": "^\\d+$",
          "min": 0,
          "max": 1.0
        }
      ] 
    },
    "6_relationship_validity": {
      "description": "온도와 복사 에너지 간의 논리적 경향성 체크",
      "rules": [
        { "name": "플랑크법칙_경향성", "formula": "not (Temperature_C > 25 and 640 < 0.05)" }
      ]
    }
  }
}
